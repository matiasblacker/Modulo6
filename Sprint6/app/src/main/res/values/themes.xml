<resources>
    <style name="Base.Theme.SprintModulo6" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/colorTextIcons</item>

        <!-- Secondary colors (Accent) -->
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorOnSecondary">@color/colorTextIcons</item>

        <!-- Background colors -->
        <item name="android:windowBackground">@color/colorPrimaryLight</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>

        <!-- Divider color -->
        <item name="colorSurface">@color/colorDivider</item>
    </style>
</resources>
